[project]
name = "skyglance"
version = "0.3.1"
description = "Pimoroni Badger 2040 W METAR-driven aviation weather display"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
    "pytest==8.3.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
